{
    "name": "selection-info",
    "displayName": "selection-info",
    "description": "",
    "version": "0.10.0",
    "publisher": "akiega1",
    "license": "MIT",
    "repository": "https://github.com/AkiEga/vscode-extension-section-info.git",
    "engines": {
        "vscode": "^1.23.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:extension.getSelectionInfo"
    ],
    "main": "./out/extension",
    "contributes": {
        "commands": [
            {
                "command": "extension.getSelectionInfo",
                "title": "Get selection info"
            }
        ],
        "menus": {
            "editor/context": [
                {
                    "command": "extension.getSelectionInfo",
                    "when": "editorHasSelection",
                    "group": "navigation"
                }
            ]
        }
    },
    "scripts": {
        "vscode:publish": "vsce publish minor -p `cat ./PackageAccessToken.txt` --yarn",
        "vscode:prepublish": "yarn run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "yarn compile && node ./node_modules/vscode/bin/test"
    },
    "devDependencies": {
        "@types/mocha": "^2.2.42",
        "@types/node": "^7.0.43",
        "typescript": "^2.9.2",
        "vscode": "^1.1.18"
    },
    "dependencies": {
        "copy-paste": "^1.3.0",
        "es6-template-strings": "^2.0.1",
        "npm": "^6.1.0"
    }
}
